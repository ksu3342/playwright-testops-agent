services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      HEADLESS: "true"
      LLM_PROVIDER: "${LLM_PROVIDER:-mock}"
      LLM_LIVE_BASE_URL: "${LLM_LIVE_BASE_URL:-}"
      LLM_LIVE_MODEL: "${LLM_LIVE_MODEL:-}"
      LLM_LIVE_API_KEY: "${LLM_LIVE_API_KEY:-}"
    volumes:
      - ./data:/app/data
      - ./generated:/app/generated
